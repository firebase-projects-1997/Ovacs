import 'package:flutter/material.dart';
import 'package:provider/provider.dart';

class SpaceSwitchingProvider extends ChangeNotifier {
  final String spaceId = '';
  changeSpace(BuildContext context) {
    context.read();
  }
}
